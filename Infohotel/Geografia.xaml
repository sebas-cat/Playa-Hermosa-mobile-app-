<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Microsoft.Maui.Controls.Maps;assembly=Microsoft.Maui.Controls.Maps"
             x:Class="Infohotel.Geografia"
             Title="Ubicación"
             BackgroundColor="#F3F3EE">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="0">

            <!-- Título principal -->
            <Frame BackgroundColor="#735945" 
                   CornerRadius="0"
                   Padding="20,15">
                <Label Text="📍 Ubicación del Hotel"
                       FontSize="22"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center"/>
            </Frame>

            <!-- MAPA DE GOOGLE MAPS -->
            <Frame Padding="0" 
       CornerRadius="12" 
       Margin="15,0,15,0"
       HasShadow="True">
                <Grid HeightRequest="300">
                    <maps:Map x:Name="mapaHotel"
                  MapType="Hybrid"
                  IsShowingUser="False"
                  IsScrollEnabled="True"
                  IsZoomEnabled="True">

                        <!-- DEJAR VACÍO los Pins aquí - se agregarán en el code-behind -->
                        <maps:Map.Pins>
                            <!-- Vacío - se llenará desde el código -->
                        </maps:Map.Pins>

                    </maps:Map>

                    <!-- Botón centrar mapa -->
                    <Button Text="📍"
                FontSize="20"
                BackgroundColor="White"
                TextColor="#735945"
                WidthRequest="50"
                HeightRequest="50"
                CornerRadius="25"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="0,0,15,15"
                Clicked="OnCenterMapClicked"/>
                </Grid>
            </Frame>

            <!-- Resto del código sigue igual... -->
            <!-- Mantén el resto del XAML tal como estaba -->

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>